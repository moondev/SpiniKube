apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: delay
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: delay
    spec:
      containers:
      - name: delay
        image: quay.io/spinnaker/front50
        ports:
        - containerPort: 8080
        command:
        - "/bin/sh"
        - "-c"
        - "sleep 3; /opt/front50/bin/front50"